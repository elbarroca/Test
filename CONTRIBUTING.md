# Contributing

Keep changes focused and dependency-free. Add or update tests with behavior
changes, then run:

```sh
python -m unittest discover -s tests -v
python -m src.manifest examples/manifest.json
```
